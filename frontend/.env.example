# Backend API URL (deployed CF app)
VITE_API_URL=https://capacity-backend.apps.example.com

# For local development with local backend
# VITE_API_URL=http://localhost:8080

# Note: Never commit your .env file with actual credentials to git!
# The .gitignore is already configured to exclude .env files
