---
applications:
  - name: capacity-ui
    memory: 64M
    instances: 1
    buildpacks:
      - staticfile_buildpack
    path: dist
    # VITE_API_URL is baked into the build via frontend/.env
    # Created by deploy.sh before npm run build
