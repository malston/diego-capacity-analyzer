---
phase: 04.1-chat-endpoint-hardening
plan: 01
subsystem: api
tags: [auth, chat, sse, config, mutex, tdd]

# Dependency graph
requires:
  - phase: 04-chat-endpoint
    provides: SSE streaming chat handler, AI provider wiring, context builder
provides:
  - Handler-level auth guard on chat endpoint (independent of AUTH_MODE)
  - Per-user scenario comparison state wired into AI context
  - Explicit MaxTokens=4096 and Model defaults for Anthropic provider
  - AIModel config field loaded from AI_MODEL env var
affects: [05-chat-panel-core, 06-chat-panel-ux]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Handler-level auth guard: GetUserClaims nil check before business logic"
    - "Per-user state on Handler struct: typed map with sync.RWMutex"
    - "Lock ordering: infraMutex before userScenariosMutex, never held simultaneously"

key-files:
  created: []
  modified:
    - backend/handlers/chat.go
    - backend/handlers/handlers.go
    - backend/handlers/scenario.go
    - backend/handlers/chat_test.go
    - backend/config/config.go
    - backend/config/config_test.go
    - backend/main.go
    - .env.example

key-decisions:
  - "sync.RWMutex for per-user scenario map (matches existing infraMutex pattern, typed map with read/write distinction)"
  - "Auth check is first validation in Chat(), before nil provider check"
  - "Scenario stored per-user keyed on claims.Username; not stored for anonymous users"
  - "Test assertion for scenario prompt checks specific table row content, not absence of placeholder marker (which also appears in static prompt)"

patterns-established:
  - "Handler-level auth guard: check GetUserClaims(r) == nil at top of handler for endpoints requiring auth regardless of global AUTH_MODE"
  - "Cross-handler state sharing: write in one handler (CompareScenario), read in another (buildChatSystemPrompt), linked via typed map on Handler struct"

requirements-completed: [CTX-03, CHAT-02, PROV-02]

# Metrics
duration: 9min
completed: 2026-02-24
---

# Phase 4.1 Plan 01: Chat Endpoint Hardening Summary

**Handler-level auth guard on chat endpoint, per-user scenario wiring into AI context, and explicit MaxTokens/Model defaults for Anthropic provider**

## Performance

- **Duration:** 9 min
- **Started:** 2026-02-24T22:52:17Z
- **Completed:** 2026-02-24T23:01:56Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments

- Chat endpoint returns JSON 401 for unauthenticated requests regardless of global AUTH_MODE setting
- Scenario comparison results stored per-user and rendered in AI system prompt context
- Anthropic provider initialized with explicit MaxTokens=4096 and configurable model via AI_MODEL env var
- All existing tests updated with user claims injection, zero regressions
- Race detector clean on all mutex-protected state

## Task Commits

Each task was committed atomically:

1. **Task 1: RED -- failing tests** - `b4212b3` (test)
2. **Task 2: GREEN -- implementation** - `3578026` (feat)
3. **Task 3: REFACTOR -- cleanup and docs** - `ddb3401` (refactor)

## Files Created/Modified

- `backend/handlers/chat.go` - Auth guard at top of Chat(), buildChatSystemPrompt accepts username, reads per-user scenario
- `backend/handlers/handlers.go` - userScenarios map and userScenariosMutex on Handler struct
- `backend/handlers/scenario.go` - Store scenario result per-user after CompareScenario
- `backend/handlers/chat_test.go` - Auth guard tests, scenario wiring test, all existing tests updated with user claims
- `backend/config/config.go` - AIModel field loaded from AI_MODEL env var
- `backend/config/config_test.go` - AIModel default and env override tests
- `backend/main.go` - Explicit ChatConfig with MaxTokens=4096 and Model from config
- `.env.example` - AI Provider section with AI_MODEL documentation

## Decisions Made

- Used sync.RWMutex for per-user scenario map (matches existing infraMutex pattern; typed map benefits from read/write distinction)
- Auth check placed as first validation in Chat(), before nil provider check (unauthenticated users should never see 503)
- Test assertion for scenario checks specific table row content ("| Cells | 6 | 10 | +4 |") rather than absence of "No scenario comparison has been run" marker, because that string also appears in the static prompt's data_gap_handling section
- Skipped adding AI_MODEL to health endpoint (keeps phase minimal; health endpoint reports availability, not configuration audit)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed overly broad test assertion for scenario prompt**

- **Found during:** Task 2 (GREEN implementation)
- **Issue:** Test checked that "No scenario comparison has been run" was absent from the system prompt, but that exact string exists in the static prompt's data_gap_handling section as a documentation marker
- **Fix:** Changed assertion to check for specific scenario table row content ("| Cells | 6 | 10 | +4 |") and "Current vs proposed capacity changes" instead
- **Files modified:** backend/handlers/chat_test.go
- **Verification:** Test passes and correctly validates scenario data presence
- **Committed in:** 3578026 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug in test assertion)
**Impact on plan:** Minimal -- test logic was sound, assertion needed refinement to account for static prompt content

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Backend streaming pipeline complete with all three gaps closed
- Auth enforcement, scenario context, and provider defaults all verified
- Ready for Phase 5: Chat Panel Core (frontend work)

## Self-Check: PASSED

All 8 modified files verified on disk. All 3 task commits verified in git log.

---

_Phase: 04.1-chat-endpoint-hardening_
_Completed: 2026-02-24_
