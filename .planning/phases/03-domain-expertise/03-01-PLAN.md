---
phase: 03-domain-expertise
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/services/ai/prompt.go
  - backend/services/ai/prompt_test.go
autonomous: true
requirements: [DOM-01, DOM-02, DOM-03, DOM-04]

must_haves:
  truths:
    - "System prompt contains TAS/Diego capacity planning heuristics (N-1, HA Admission Control, vCPU:pCPU ratios, cell sizing, isolation segments, Diego auction, Small Footprint)"
    - "System prompt frames findings in procurement terms (lead times, budget cycles, growth planning, concrete hardware quantities)"
    - "System prompt teaches the LLM to acknowledge material data gaps using the exact BuildContext markers (NOT CONFIGURED, UNAVAILABLE, No scenario comparison)"
    - "System prompt instructs the LLM to cite specific data values from infrastructure context when making claims"
    - "Combined system prompt (static + BuildContext) stays within token budget (static under ~2500 tokens, combined under ~3500 tokens)"
  artifacts:
    - path: "backend/services/ai/prompt.go"
      provides: "Static system prompt const and BuildSystemPrompt composition function"
      exports: ["BuildSystemPrompt"]
      contains: "const systemPrompt"
    - path: "backend/services/ai/prompt_test.go"
      provides: "Content-assertion tests validating all four DOM requirements plus token budget and composition"
      contains: "TestSystemPrompt"
  key_links:
    - from: "backend/services/ai/prompt.go"
      to: "backend/services/ai/context.go"
      via: "BuildSystemPrompt wraps BuildContext output in <infrastructure_context> XML tags"
      pattern: "infrastructure_context"
    - from: "backend/services/ai/prompt.go"
      to: "backend/services/ai/options.go"
      via: "Phase 4 will pass BuildSystemPrompt result to WithSystem option"
      pattern: "WithSystem"
---

<objective>
System prompt const encoding TAS/Diego capacity planning domain expertise, procurement framing, data gap handling, and evidence requirements. Composition function wrapping BuildContext output for per-request injection.

Purpose: Transform the LLM from a generic chatbot into a domain expert that reasons about capacity in procurement terms using the operator's live data.
Output: `prompt.go` with const + `BuildSystemPrompt()`, `prompt_test.go` with content-assertion and composition tests.
</objective>

<execution_context>
@/Users/markalston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markalston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-domain-expertise/03-RESEARCH.md
@.planning/phases/02-context-builder/02-01-SUMMARY.md
@backend/services/ai/context.go
@backend/services/ai/provider.go
@backend/services/ai/options.go
</context>

<feature>
  <name>System Prompt with Domain Expertise and Composition</name>
  <files>backend/services/ai/prompt.go, backend/services/ai/prompt_test.go</files>
  <behavior>
    Content requirements (all verified by string-contains assertions on the const):
    - DOM-01: Contains domain knowledge sections covering N-1 redundancy, HA Admission Control (25-33%), vCPU:pCPU ratios (4:1 safe, 8:1 risky), cell sizing (32-64GB), utilization targets (80% HIGH, 90% CRITICAL), free chunks/fragmentation, isolation segments (min 4 cells), Diego auction mechanics, Small Footprint TAS
    - DOM-02: Contains procurement framing with lead times (8-12 weeks), budget cycles, growth horizons (6-12 months), concrete quantity recommendations
    - DOM-03: References exact BuildContext markers: "NOT CONFIGURED", "UNAVAILABLE", "No scenario comparison has been run". Instructs materiality-based acknowledgment (only mention when relevant to question)
    - DOM-04: Instructs citing specific numbers from infrastructure context (cell counts, utilization percentages, memory values)

    Structural requirements:
    - Uses XML tags for sections: domain_knowledge, procurement_framing, response_rules, data_gap_handling
    - Static prompt under 10000 characters (~2500 tokens)
    - Measured language per Claude 4.6 best practices (no excessive MUST/ALWAYS/NEVER emphasis)
    - Tone: senior engineer capacity review notes, no conversational filler, no persona

    Composition function:
    - BuildSystemPrompt("context data") -> static prompt + "\n\n<infrastructure_context>\ncontext data\n</infrastructure_context>"
    - BuildSystemPrompt("") -> still includes infrastructure_context tags with empty body
    - Composed result contains both static domain knowledge and the context data

    Test cases:
    - TestSystemPromptContainsDomainKnowledge: all 4 XML section tag pairs present
    - TestSystemPromptContainsHeuristics: N-1, HA Admission Control, vCPU:pCPU, isolation segment, cell sizing, Diego (case-insensitive)
    - TestSystemPromptContainsProcurementFraming: lead time, budget, procurement terms
    - TestSystemPromptContainsGapHandling: NOT CONFIGURED, UNAVAILABLE, No scenario comparison markers
    - TestSystemPromptContainsEvidenceRequirement: cite or reference instruction present
    - TestSystemPromptTokenBudget: static prompt under 10000 chars
    - TestBuildSystemPromptIncludesContext: composed result has infrastructure_context tags, context data, and domain knowledge
    - TestBuildSystemPromptEmptyContext: empty context still produces valid structure

  </behavior>
  <implementation>
    prompt.go:
    - ABOUTME comments following package convention (2 lines describing file purpose)
    - package ai (same package as provider.go, context.go, options.go)
    - const systemPrompt = backtick-delimited raw string with XML-structured sections
    - Opening line: role definition (one sentence, no persona name)
    - Sections in order: domain_knowledge, procurement_framing, response_rules, data_gap_handling
    - Closing line: transition sentence pointing to the infrastructure_context that follows
    - func BuildSystemPrompt(context string) string -- concatenates const with XML-wrapped context
    - Domain knowledge content: use research Code Examples section as the primary reference for prompt content. The research provides the exact section structure and heuristic thresholds. Match the thresholds to the existing codebase values: utilizationFlag() at 80%/90%, vcpuRatioFlag() at 4:1/8:1.
    - Tone guidance: "Write like a senior engineer's capacity review notes" per user decision. No "Great question" or "I'd be happy to help." Per Claude 4.6 best practices, use measured language -- not "you MUST ALWAYS cite" but "cite specific numbers."
    - Gap handling: general rules plus materiality examples per research recommendation (Open Question 1). Reference the 3 exact BuildContext markers.

    prompt_test.go:
    - ABOUTME comments following package convention
    - package ai (same package, accesses unexported systemPrompt const)
    - 8 test functions as listed in behavior section
    - Use strings.Contains and strings.ToLower for content assertions
    - No mocks needed -- pure string content validation

  </implementation>
</feature>

<verification>
```bash
cd /Users/markalston/code/diego-capacity-analyzer && go test ./backend/services/ai/ -run TestSystemPrompt -v && go test ./backend/services/ai/ -run TestBuildSystemPrompt -v
```

All 8 prompt tests pass. Full ai package test suite still passes:

```bash
go test ./backend/services/ai/ -v -count=1
```

</verification>

<success_criteria>

- All 8 prompt content and composition tests pass
- All existing ai package tests (context, provider, options, anthropic) still pass
- systemPrompt const is under 10000 characters
- BuildSystemPrompt correctly wraps context in infrastructure_context XML tags
- Static prompt covers all four DOM requirements verified by content assertions
  </success_criteria>

<output>
After completion, create `.planning/phases/03-domain-expertise/03-01-SUMMARY.md`
</output>
