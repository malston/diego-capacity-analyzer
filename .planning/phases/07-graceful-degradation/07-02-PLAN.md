---
phase: 07-graceful-degradation
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - frontend/src/components/chat/ChatMessages.jsx
  - frontend/src/components/chat/ChatPanel.jsx
  - frontend/src/components/chat/ChatPanel.test.jsx
autonomous: false
requirements:
  - DEG-01
  - DEG-02
  - DEG-03

must_haves:
  truths:
    - "When BOSH or vSphere data sources are unavailable, a persistent info banner appears at the top of the chat panel listing the missing sources"
    - "When all data sources are available, no banner appears"
    - "Starter prompts only show questions relevant to available data sources"
    - "At least 3 prompts always appear in the empty state regardless of data source availability"
    - "CF-only environments show app-level analysis prompts instead of BOSH/vSphere-dependent ones"
  artifacts:
    - path: "frontend/src/components/chat/ChatMessages.jsx"
      provides: "Tagged prompt data with requires field, filtering function, and DataSourceBanner component"
      contains: "requires"
    - path: "frontend/src/components/chat/ChatPanel.jsx"
      provides: "Health fetch on mount for data_sources, passes dataSources to ChatMessages"
      contains: "data_sources"
    - path: "frontend/src/components/chat/ChatPanel.test.jsx"
      provides: "Tests for banner visibility, prompt filtering, and data source fetch"
      contains: "data_sources"
  key_links:
    - from: "frontend/src/components/chat/ChatPanel.jsx"
      to: "/api/v1/health"
      via: "fetch on mount"
      pattern: "api/v1/health"
    - from: "frontend/src/components/chat/ChatPanel.jsx"
      to: "frontend/src/components/chat/ChatMessages.jsx"
      via: "dataSources prop"
      pattern: "dataSources"
    - from: "frontend/src/components/chat/ChatMessages.jsx"
      to: "STARTER_PROMPTS data"
      via: "getAvailablePrompts filter function"
      pattern: "getAvailablePrompts"
---

<objective>
Add a data source availability banner and adaptive starter prompts to the chat panel.

Purpose: Operators in CF-only environments (no BOSH, no vSphere) need to know what data the advisor can and cannot analyze. A persistent banner communicates missing sources, and starter prompts adapt to only suggest questions the advisor can answer with available data.

Output: Updated ChatPanel with health fetch, DataSourceBanner component, tagged prompt data with filtering, and comprehensive tests.
</objective>

<execution_context>
@/Users/markalston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markalston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-graceful-degradation/07-CONTEXT.md
@.planning/phases/07-graceful-degradation/07-RESEARCH.md
@.planning/phases/07-graceful-degradation/07-01-SUMMARY.md

@frontend/src/components/chat/ChatPanel.jsx
@frontend/src/components/chat/ChatMessages.jsx
@frontend/src/components/chat/ChatPanel.test.jsx
@frontend/src/TASCapacityAnalyzer.jsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From backend health endpoint (after Plan 07-01):
```json
{
  "cf_api": "ok",
  "bosh_api": "not_configured",
  "ai_configured": true,
  "cache_status": { "cells_cached": false, "apps_cached": false },
  "data_sources": {
    "bosh": true,
    "vsphere": false,
    "log_cache": false
  }
}
```

From ChatPanel.jsx (current props):
```jsx
const ChatPanel = ({ isOpen, onClose }) => { ... }
```

From ChatMessages.jsx (current props):
```jsx
const ChatMessages = React.memo(
  ({ messages, isStreaming, error, onRetry, onPromptClick }) => { ... }
);
```

From TASCapacityAnalyzer.jsx (mount pattern):
```jsx
{chatOpen && (
  <ChatPanel isOpen={chatOpen} onClose={() => setChatOpen(false)} />
)}
```
ChatPanel is conditionally rendered (unmounts on close, remounts on open).
This means useEffect with [] deps fires on each panel open.

From useChatStream hook:
```jsx
const { messages, isStreaming, error, sendMessage, clearConversation, retryLastMessage } = useChatStream();
```

From ChatPanel.test.jsx (mock pattern):
```jsx
vi.mock("../../hooks/useChatStream", () => ({
  useChatStream: vi.fn(() => ({ ... })),
}));
global.fetch = vi.fn().mockResolvedValue({ ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tagged prompts, filtering function, and data source banner</name>
  <files>frontend/src/components/chat/ChatMessages.jsx, frontend/src/components/chat/ChatPanel.jsx, frontend/src/components/chat/ChatPanel.test.jsx</files>
  <behavior>
    - Test: ChatMessages renders only CF-appropriate prompts when dataSources={{ bosh: false, vsphere: false, log_cache: false }}
    - Test: ChatMessages renders BOSH-dependent prompts when dataSources={{ bosh: true, vsphere: false, log_cache: false }}
    - Test: ChatMessages renders all prompts (including vsphere-dependent) when dataSources={{ bosh: true, vsphere: true, log_cache: true }}
    - Test: ChatMessages always renders at least 3 starter prompt chips regardless of dataSources
    - Test: ChatMessages does not render a BOSH-dependent prompt ("Assess current capacity") when bosh is false
    - Test: ChatMessages does not render a vsphere-dependent prompt ("Check HA readiness") when vsphere is false
    - Test: When dataSources is null (health not loaded yet), ChatMessages renders the full original set of prompts as fallback
    - Test: DataSourceBanner renders "BOSH data unavailable" when only BOSH is missing
    - Test: DataSourceBanner renders "BOSH and vSphere data unavailable" when both are missing
    - Test: DataSourceBanner does not render when all sources are available (bosh: true, vsphere: true)
    - Test: DataSourceBanner does not render when dataSources is null (health not loaded yet -- no banner before data arrives)
    - Test: DataSourceBanner does not include "Log Cache" in banner text (log_cache is excluded from banner per decision)
    - Test: ChatPanel fetches /api/v1/health on mount and passes data_sources to ChatMessages as dataSources prop
    - Test: ChatPanel renders DataSourceBanner between header and ChatMessages
    - Test: When health fetch fails, ChatPanel assumes degraded (all sources false) and banner appears
  </behavior>
  <action>
RED: Write tests first in `ChatPanel.test.jsx`. Add new describe blocks:

1. `describe("ChatMessages - Adaptive starter prompts")` -- Tests for prompt filtering based on dataSources prop. Render ChatMessages with `messages=[]` and various `dataSources` values. Assert which prompt labels appear/don't appear.

2. `describe("DataSourceBanner")` -- Import and test the banner component directly. Pass different dataSources values, assert on rendered text.

3. Add tests to the existing `describe("ChatPanel")` block for health fetch behavior. Mock `global.fetch` to return health response with `data_sources`. Verify the banner appears when sources are missing. Verify fetch is called on mount.

For prompt filtering tests, ChatMessages needs a new `dataSources` prop. The tests will fail because the prop doesn't exist yet.

GREEN: Implement changes in order:

**ChatMessages.jsx changes:**
1. Replace the static `STARTER_PROMPTS` array with `ALL_PROMPTS` -- an expanded array where each prompt has a `requires` field:
   - "Assess current capacity" -- `requires: ["bosh"]`
   - "Plan for growth" -- `requires: ["bosh"]`
   - "Review cell sizing" -- `requires: ["bosh"]`
   - "Check HA readiness" -- `requires: ["bosh", "vsphere"]`
   - "Review app distribution" -- `requires: ["cf"]` (CF-only fallback)
   - "Analyze memory allocation" -- `requires: ["cf"]` (CF-only fallback)
   - "Check isolation segments" -- `requires: ["cf"]` (CF-only fallback)
   - "Assess app density" -- `requires: ["cf"]` (CF-only fallback)

   Exact question text for CF-only prompts:
   - "Review app distribution": "Analyze how applications are distributed across isolation segments. Are there any segments that are over or under-utilized?"
   - "Analyze memory allocation": "Review the memory allocation patterns across our applications. Are there apps that are over-provisioned or under-provisioned relative to their actual usage?"
   - "Check isolation segments": "How are our isolation segments configured and which applications run in each? Are there any segmentation improvements we should consider?"
   - "Assess app density": "What is the current application density? Are there any apps consuming significantly more resources than others?"

2. Add `getAvailablePrompts(dataSources, maxPrompts = 4)` function:
   - Build a Set of available sources (always includes "cf"; add "bosh" if `dataSources?.bosh`, "vsphere" if `dataSources?.vsphere`)
   - Filter `ALL_PROMPTS` to those whose `requires` array entries are all in the available set
   - Return first `maxPrompts` entries
   - If `dataSources` is null (health not loaded), return the first 4 prompts from ALL_PROMPTS (original behavior as fallback -- these happen to be the BOSH-dependent ones, matching the pre-Phase-7 behavior)

3. Add `dataSources` to the ChatMessages component props. In the empty-state render, replace `STARTER_PROMPTS.map(...)` with `getAvailablePrompts(dataSources).map(...)`.

4. Add a `DataSourceBanner` component inline in ChatMessages.jsx (not a separate file -- it's ~15 lines and tightly coupled to the chat panel):
   - Accepts `dataSources` prop
   - If `dataSources` is null, return null (no banner until health loads)
   - Build `missing` array: push "BOSH" if `!dataSources.bosh`, push "vSphere" if `!dataSources.vsphere` (do NOT include log_cache)
   - If `missing.length === 0`, return null
   - Render: `<div className="px-4 py-2 bg-amber-500/10 border-b border-amber-500/20 text-amber-300 text-xs flex items-center gap-2 flex-shrink-0">` with Info icon from lucide-react and text `${missing.join(" and ")} data unavailable`
   - Non-dismissible (no close button)
   - Export `DataSourceBanner` as a named export for direct testing

**ChatPanel.jsx changes:**
1. Add `useState` for `dataSources` (initial value `null`)
2. Add `useEffect` with `[]` deps (fires on mount, which happens each time panel opens because parent conditionally renders with `chatOpen &&`):
   - Fetch `${apiURL}/api/v1/health` with `credentials: "include"`
   - On success: `setDataSources(data.data_sources || null)`
   - On failure: `setDataSources({ bosh: false, vsphere: false, log_cache: false })` (assume degraded)
3. Import and render `DataSourceBanner` between the header div and ChatMessages
4. Pass `dataSources` to both `DataSourceBanner` and `ChatMessages`

Run all tests -- they should pass.

REFACTOR: Export `getAvailablePrompts` for direct unit testing if the test approach requires it. Otherwise keep it module-private.
  </action>
  <verify>
    <automated>cd /Users/markalston/code/diego-capacity-analyzer/frontend && npx vitest run src/components/chat/ChatPanel.test.jsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>
- ChatMessages accepts `dataSources` prop and filters starter prompts accordingly
- DataSourceBanner renders a persistent amber info banner listing missing BOSH/vSphere sources
- ChatPanel fetches health on mount, passes data_sources to children
- All 4 original starter prompt labels still appear when all sources are available
- CF-only fallback prompts appear when BOSH/vSphere are unavailable
- At least 3 prompts always visible in empty state
- Banner disappears when all sources available
- All existing ChatPanel tests continue to pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify graceful degradation UX</name>
  <files>frontend/src/components/chat/ChatPanel.jsx</files>
  <action>
Human verifies the data source banner and adaptive starter prompts work correctly in a real environment with different data source configurations.
  </action>
  <verify>
1. Start the backend WITHOUT BOSH/vSphere configured (only CF_API_URL, CF_USERNAME, CF_PASSWORD, AI_PROVIDER, AI_API_KEY):
   `make backend-run`
2. Start the frontend dev server:
   `make frontend-dev`
3. Open http://localhost:5173 and click the AI Advisor toggle button
4. Verify: An amber banner at the top of the chat panel says "BOSH and vSphere data unavailable"
5. Verify: Starter prompts show CF-appropriate questions (e.g., "Review app distribution", "Analyze memory allocation") instead of BOSH-dependent ones
6. Verify: At least 3-4 prompt chips are visible
7. Verify: Click a starter prompt -- it sends the question and the advisor responds acknowledging limited data
8. If possible, start the backend WITH BOSH configured (add BOSH_ENVIRONMENT, BOSH_CLIENT, BOSH_CLIENT_SECRET):
   - Verify: Banner shows only "vSphere data unavailable" (not BOSH)
   - Verify: BOSH-dependent prompts (e.g., "Assess current capacity") reappear
9. If all sources are configured, verify no banner appears and all original prompts show

Resume: Type "approved" to complete Phase 7, or describe any issues to address
  </verify>
  <done>Human confirms: amber banner shows/hides correctly based on data sources, starter prompts adapt to available data, CF-only prompts appear when BOSH/vSphere are unavailable, banner disappears when all sources are available</done>
</task>

</tasks>

<verification>
```bash
# Backend tests (including new health data_sources tests from Plan 07-01)
cd /Users/markalston/code/diego-capacity-analyzer/backend && go test ./handlers/ -v -count=1

# Frontend tests (all chat component tests)
cd /Users/markalston/code/diego-capacity-analyzer/frontend && npx vitest run src/components/chat/ --reporter=verbose

# Full test suite
cd /Users/markalston/code/diego-capacity-analyzer && make check
```
</verification>

<success_criteria>
- Amber info banner appears when BOSH and/or vSphere data sources are unavailable
- Banner disappears when all sources are available
- Starter prompts adapt to available data (no BOSH-dependent prompts in CF-only mode)
- At least 3-4 prompts always visible in empty state
- Log Cache never mentioned in banner text
- All existing tests pass alongside new tests
- Visual verification confirms the banner and prompts work correctly in a real environment
</success_criteria>

<output>
After completion, create `.planning/phases/07-graceful-degradation/07-02-SUMMARY.md`
</output>
