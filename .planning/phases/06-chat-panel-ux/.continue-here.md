---
phase: 06-chat-panel-ux
task: PR review fixes
total_tasks: 5
status: in_progress
last_updated: 2026-03-03T15:29:18.258Z
---

<current_state>
Phase 6 execution is COMPLETE (2/2 plans done, verification passed, roadmap updated).
PR #133 was created and pushed. A 5-agent parallel review found 5 important issues.
RED phase (failing tests) is committed. GREEN phase (implementation) is next.
</current_state>

<completed_work>

- Plan 06-01: Error classification, clearConversation, retryLastMessage in transport/hook layers - DONE
- Plan 06-02: Loading dots, inline errors, reset button, starter prompts in UI - DONE
- Phase verification: 18/18 must-haves passed - DONE
- PR #133 created and pushed - DONE
- 5-agent parallel PR review completed - DONE
- RED phase: 5 failing tests committed (f46106e) - DONE
</completed_work>

<remaining_work>

GREEN phase -- implement fixes for 5 issues (tests already written and failing):

1. **retryLastMessage race condition + duplicate user message** (useChatStream.js:108-136)
   - Remove BOTH last user AND last assistant messages before retry
   - Manually sync messagesRef.current before calling sendMessage
   - Tests: "removes both the last user and assistant messages on retry" and "does not include stale messages in conversation sent to backend on retry"

2. **sendMessage doesn't abort existing stream** (useChatStream.js:56)
   - Add `if (abortRef.current) abortRef.current.abort();` before creating new AbortController
   - Test: "aborts existing stream when sendMessage is called again"

3. **Plain Error instead of ChatError for null response.body** (chatApi.js:98-100)
   - Change `throw new Error(...)` to `throw new ChatError(..., "server")`
   - Test: "throws ChatError when response.body is null"

4. **Mid-stream TypeError not classified as network** (chatApi.js:106-124)
   - Wrap reader.read() loop to catch TypeError and throw ChatError("Connection lost", "network")
   - Test: "throws ChatError with type 'network' when reader.read() throws TypeError mid-stream"

5. **Nested ternary in ChatMessage.jsx** (suggestion from code-simplifier)
   - Extract to MessageContent component with if/else
   - No new test needed (existing tests cover behavior)

Also needed after GREEN:
- REFACTOR phase
- Update ABOUTME comment in useChatStream.js ("error classification" -> "error propagation")
- Commit, push, re-run targeted tests to verify
</remaining_work>

<decisions_made>

- MessageSquarePlus icon chosen for "new conversation" button (replacing RotateCcw which looked like reload)
- Phase 6 marked complete in roadmap despite these PR review fixes being outstanding
- All 5 review agents agreed retryLastMessage is the main hotspot
- Fix approach: remove both user+assistant on retry, sync ref manually, abort existing streams
</decisions_made>

<blockers>
None -- straightforward implementation work remaining
</blockers>

<context>
The PR review found that retryLastMessage has a race condition where messagesRef.current
is stale when sendMessage reads it (because setMessages hasn't flushed yet), AND it
leaves the original user message in place causing duplicates. The fix is to:
1. Remove both last user + assistant messages
2. Manually update messagesRef.current = cleaned array before calling sendMessage
3. Also add abort-existing-stream guard to sendMessage itself

The chatApi fixes are one-liners: ChatError instead of Error for null body,
and wrapping the reader.read() loop to catch TypeError for mid-stream network drops.
</context>

<next_action>
Start with: Implement GREEN phase fixes in useChatStream.js (issues 1-2-3), then chatApi.js (issues 4-5), then ChatMessage.jsx (issue 6). Run tests after each file. All 5 failing tests should turn green. Then REFACTOR, update ABOUTME, commit, and push to PR #133.
</next_action>
