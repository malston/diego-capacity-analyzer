---
applications:
  - name: capacity-backend
    memory: 256M
    instances: 1
    buildpacks:
      - go_buildpack
    env:
      CACHE_TTL: 300
    # All other env vars set by deploy.sh via cf set-env:
    # CF_API_URL, CF_USERNAME, CF_PASSWORD
    # BOSH_ENVIRONMENT, BOSH_CLIENT, BOSH_CLIENT_SECRET, BOSH_DEPLOYMENT, BOSH_CA_CERT
