# ABOUTME: Example environment configuration for diego-capacity-analyzer
# ABOUTME: Copy to .env and fill in values, or use generate-env.sh to derive from Ops Manager

# =============================================================================
# Cloud Foundry API (Required)
# =============================================================================
CF_API_URL=https://api.sys.example.com
CF_USERNAME=admin
CF_PASSWORD=

# =============================================================================
# BOSH Director (Optional - enables Diego cell metrics)
# =============================================================================
BOSH_ENVIRONMENT=https://10.0.0.6:25555
BOSH_CLIENT=ops_manager
BOSH_CLIENT_SECRET=
BOSH_CA_CERT=
BOSH_DEPLOYMENT=cf-abc123

# Optional: SSH proxy for non-routable BOSH networks
# BOSH_ALL_PROXY=ssh+socks5://ubuntu@opsman.example.com:22?private-key=/path/to/key

# =============================================================================
# vSphere (Optional - enables infrastructure discovery)
# =============================================================================
VSPHERE_HOST=vcenter.example.com
VSPHERE_DATACENTER=Datacenter-Name
VSPHERE_USERNAME=administrator@vsphere.local
VSPHERE_PASSWORD=
# VSPHERE_INSECURE=true

# =============================================================================
# Ops Manager (Optional - for generate-env.sh)
# =============================================================================
OM_TARGET=opsman.example.com
OM_USERNAME=admin
OM_PASSWORD=
# Or use client credentials:
# OM_CLIENT_ID=
# OM_CLIENT_SECRET=
OM_SKIP_SSL_VALIDATION=false
OM_CONNECT_TIMEOUT=60

# =============================================================================
# CredHub (Optional - derived from Ops Manager)
# =============================================================================
# CREDHUB_SERVER=
# CREDHUB_CLIENT=
# CREDHUB_SECRET=
# CREDHUB_CA_CERT=
# CREDHUB_PROXY=

# =============================================================================
# Frontend Configuration
# =============================================================================
VITE_CF_API_URL=https://api.sys.example.com
VITE_CF_UAA_URL=https://login.sys.example.com
# VITE_BOSH_URL=https://10.0.0.6:25555

# =============================================================================
# Server Tuning (Optional)
# =============================================================================
# PORT=8080
# CACHE_TTL=300
# DASHBOARD_CACHE_TTL=30
# VSPHERE_CACHE_TTL=300
# LOG_LEVEL=info
# LOG_FORMAT=text

# =============================================================================
# Authentication (Optional)
# =============================================================================
# AUTH_MODE controls backend JWT token validation:
#   disabled - no authentication (local development)
#   optional - validate tokens if present, allow anonymous (default)
#   required - reject unauthenticated requests
# AUTH_MODE=optional

# CORS_ALLOWED_ORIGINS=https://capacity-ui.apps.example.com,http://localhost:5173
